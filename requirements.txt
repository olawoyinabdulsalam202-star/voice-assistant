# ── Core ──
Flask==3.1.3
flask-cors==6.0.2
gunicorn==26.0.0
Werkzeug==3.1.8
Jinja2==3.1.6
MarkupSafe==3.0.3
click==8.3.3
blinker==1.9.0
colorama==0.4.6
packaging==26.2

# itsdangerous signs the auth tokens (auth.py). It was already a Flask
# transitive dependency; it is listed explicitly now because we import it.
itsdangerous==2.2.0

# ── Config / env ──
python-dotenv==1.0.1

# ── Database ──
psycopg2-binary==2.9.10

# ── HTTP ──
requests==2.33.1
urllib3==2.6.3
certifi==2026.4.22
truststore==0.10.4
charset-normalizer==3.4.7
idna==3.13
h11==0.16.0

# ── Auth ──
google-auth==2.36.0
webauthn==2.5.2
pyOpenSSL==25.1.0
dnspython==2.7.0

# ── Files ──
pdfplumber==0.11.4

# ── Server ──
uvicorn==0.44.0

# ════════════════════════════════════════════════════════════
#  NOT YET INSTALLED — needed for the next phase
#
#  redis: rate limits, login lockouts and WebAuthn challenges are
#    currently in-process dicts. Under gunicorn with N workers a limit
#    is only 1/N effective and passkey logins fail at random, because
#    the verify request can hit a different worker than the one that
#    issued the challenge. Uncomment when REDIS_URL is set (S10-S12).
# redis==5.2.1
#
#  APScheduler: expiry reminders and campaign sends. Note it must be
#    guarded by a Postgres advisory lock or run in a single dedicated
#    process — a naive in-app scheduler under 4 workers emails every
#    user 4 times (E5).
# APScheduler==3.11.0
#
#  sentry-sdk: error tracking (I2).
# sentry-sdk[flask]==2.20.0
# ════════════════════════════════════════════════════════════
